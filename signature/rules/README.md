# signature/rules 板块说明

## 板块内容
- 存放签名规则配置文件（如 YAML/JSON 格式）。
- 规则定义包含：匹配条件、风险级别、处置建议。

## 边界
- 仅存放静态规则文件，不执行逻辑。
- 规则加载与解析由后端服务完成。

## 对外接口
- 后端服务读取规则文件并加载到内存。
- 规则变更需重启服务或触发热加载。

## 关键函数/入口
- 规则加载函数：待后端服务实现。
- 规则热加载接口：待后端服务实现。