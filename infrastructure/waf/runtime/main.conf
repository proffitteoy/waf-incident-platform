# Coraza + CRS Runtime Entry

# Base engine directives.
Include ./coraza.conf

# Project-specific defaults (log format/path, mode, thresholds).
Include ./10-project-overrides.conf

# CRS setup (anomaly mode, paranoia, policy tuning).
Include ./crs-setup.conf

# CRS rules + local exclusions.
Include ./rules/*.conf
