# storage 板块说明

## 板块内容
- `logs/`：WAF 与入口日志归档。
- `pcap/`：取证抓包文件。
- `reports/`：分析报告与导出文件。

## 边界
- 存储层只保留文件产物，不存业务逻辑。
- 文件索引与审计事实以数据库记录为准。

## 对外接口
- 后端通过 `LOG_ARCHIVE_DIR`、`PCAP_DIR`、`REPORT_DIR` 配置访问。

## 关键函数/入口
- 存储板块无业务函数，目录由后端配置项驱动。
